# 📚 Инструкция по развертыванию

## 🐧 Развертывание на Linux/Mac

### 1. Подготовка

```bash
# Клонируйте или скопируйте проект
cd schedule_bot

# Создайте .env файл
cp .env.example .env
nano .env  # Отредактируйте, добавьте токен бота
```

### 2. Запуск через скрипт (рекомендуется)

```bash
# Дайте права на выполнение (если еще не сделано)
chmod +x run.sh

# Запустите бота
./run.sh
```

### 3. Ручной запуск

```bash
# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте окружение
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Создайте необходимые директории
mkdir -p data logs

# Запустите бота
python main.py
```

## 🪟 Развертывание на Windows

### 1. Подготовка

```powershell
# Перейдите в папку проекта
cd schedule_bot

# Создайте .env файл
copy .env.example .env
notepad .env  # Отредактируйте, добавьте токен бота
```

### 2. Запуск

```powershell
# Создайте виртуальное окружение
python -m venv venv

# Активируйте окружение
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Создайте необходимые директории
mkdir data
mkdir logs

# Запустите бота
python main.py
```

## 🐳 Развертывание через Docker

### 1. Подготовка

```bash
# Создайте .env файл
cp .env.example .env
nano .env  # Отредактируйте конфигурацию
```

### 2. Запуск

```bash
# Соберите и запустите контейнер
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

## 🔧 Получение токена бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "ПВГУС Расписание")
   - Введите username бота (должен заканчиваться на `bot`, например: `pvgus_schedule_bot`)
4. Скопируйте полученный токен в `.env` файл

## ⚙️ Настройка конфигурации

Откройте файл `.env` и настройте:

```env
# Токен бота от @BotFather
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# ID администраторов (через запятую)
ADMIN_IDS=123456789,987654321

# База данных
DATABASE_PATH=data/bot.db

# Логирование
LOG_LEVEL=INFO
LOG_FILE=logs/bot.log
```

## 📱 Получение своего Telegram ID

1. Откройте [@userinfobot](https://t.me/userinfobot)
2. Нажмите "Start"
3. Скопируйте ваш ID
4. Добавьте его в `ADMIN_IDS` в `.env` файле

## 🔄 Обновление бота

```bash
# Остановите бота (Ctrl+C)

# Получите обновления
git pull

# Обновите зависимости
pip install -r requirements.txt --upgrade

# Запустите снова
python main.py
```

## 🐛 Решение проблем

### Бот не запускается

1. Проверьте правильность токена в `.env`
2. Проверьте наличие всех зависимостей: `pip install -r requirements.txt`
3. Проверьте логи в папке `logs/`

### Ошибка базы данных

```bash
# Удалите старую базу данных
rm data/bot.db

# Запустите бота снова - БД создастся автоматически
python main.py
```

### Ошибки импорта

```bash
# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
```

## 🚀 Запуск в фоновом режиме (Linux)

### Использование systemd

1. Создайте файл сервиса:

```bash
sudo nano /etc/systemd/system/schedule-bot.service
```

2. Добавьте содержимое:

```ini
[Unit]
Description=PVGUS Schedule Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/schedule_bot
Environment="PATH=/path/to/schedule_bot/venv/bin"
ExecStart=/path/to/schedule_bot/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

3. Активируйте сервис:

```bash
sudo systemctl daemon-reload
sudo systemctl enable schedule-bot
sudo systemctl start schedule-bot

# Проверка статуса
sudo systemctl status schedule-bot

# Просмотр логов
sudo journalctl -u schedule-bot -f
```

### Использование screen

```bash
# Установите screen
sudo apt install screen

# Создайте новую сессию
screen -S schedule_bot

# Запустите бота
python main.py

# Отключитесь от сессии: Ctrl+A, затем D

# Вернитесь к сессии
screen -r schedule_bot
```

## 📊 Мониторинг

Логи сохраняются в:
- `logs/bot.log` - основные логи бота
- `data/bot.db` - база данных SQLite

Для просмотра логов в реальном времени:

```bash
tail -f logs/bot.log
```
